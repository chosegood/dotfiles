[user]
	name = Chris Hosegood
	email = chris.hosegood@corelogic.co.uk
[github]
  user = chosegood
[credential]
          helper = cache --timeout=3600
[color]
        ui = auto
        diff = auto
        status = auto
        branch = auto
        interactive = auto
        grep = auto
[alias]
          bs = branch -a
          lg = log --all --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
          files = show --pretty="format:" --name-only
          st = status

[core]
	pager = less -r
